import test from 'node:test';import assert from 'node:assert/strict';import vm from 'node:vm';import {readFile} from 'node:fs/promises';
test('first dashboard render does not wait for workspace, chat or reflection backfill',async()=>{const code=await readFile(new URL('../portal.js',import.meta.url),'utf8');const block=code.slice(code.indexOf('let programLoadVersion=0;'),code.indexOf('\nfunction renderClaraJourney()'));const calls=[];const context={clearClaraStepTransition(){},initialViewResolved:false,customerWorkspace:null,program:null,currentWeek:0,currentContent:null,journeyMessages:[],claraCurrentPrompt:'',safeSelectedWeek:()=>2,showView:v=>calls.push(v),render(){},renderClaraJourney(){},request:url=>url.endsWith('?fast=1')?Promise.resolve({onboardingComplete:true,week:{}}):new Promise(()=>{})};vm.createContext(context);vm.runInContext(block,context);await context.loadProgram();assert.deepEqual(calls,['today']);});
