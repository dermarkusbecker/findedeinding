import test from 'node:test';import assert from 'node:assert/strict';import {normalizeAppointmentCategories,categoryBookingSettings} from '../lib/appointment-categories.js';import {normalizeBookingSettings,isWithinBookingAvailability} from '../lib/booking-availability.js';
const categories=[{id:'intro',name:'Erstgespräch',duration:30,active:true},{id:'coaching',name:'Coaching',duration:90,active:true},{id:'off',name:'Pause',duration:45,active:false}];
test('categories require names and active unique definitions',()=>{assert.throws(()=>normalizeAppointmentCategories([{...categories[0],name:''}]));assert.throws(()=>normalizeAppointmentCategories([categories[0],categories[0]]));assert.throws(()=>categoryBookingSettings({categories},'off'));assert.throws(()=>categoryBookingSettings({categories},'unknown'));});
test('selected category duration is enforced inside common weekly availability',()=>{const settings=normalizeBookingSettings({categories,minNoticeHours:0,weeklyAvailability:{2:[{start:'09:00',end:'10:00'}]}});const start='2026-09-15T07:00:00Z',now=new Date('2026-09-15T06:00:00Z');assert.equal(isWithinBookingAvailability(start,30,categoryBookingSettings(settings,'intro'),now),true);assert.equal(isWithinBookingAvailability(start,90,categoryBookingSettings(settings,'coaching'),now),false);assert.equal(categoryBookingSettings(settings).category.name,'Erstgespräch');});
