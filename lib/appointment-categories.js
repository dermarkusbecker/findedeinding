export function normalizeAppointmentCategories(value){if(value===undefined)return[{id:'initial',name:'Klarheitsgespräch',duration:45,active:true}];if(!Array.isArray(value)||!value.length||value.length>30)throw Object.assign(new Error('Bitte 1 bis 30 Terminkategorien anlegen.'),{status:400});const seen=new Set();const categories=value.map(c=>{const id=String(c.id||''),name=String(c.name||'').trim();if(!/^[a-zA-Z0-9_-]{1,80}$/.test(id)||seen.has(id)||!name||name.length>120||![30,45,60,90].includes(Number(c.duration)))throw Object.assign(new Error('Jede Kategorie benötigt einen eindeutigen Schlüssel, Namen und eine gültige Dauer.'),{status:400});seen.add(id);return{id,name,duration:Number(c.duration),active:c.active!==false};});if(!categories.some(c=>c.active))throw Object.assign(new Error('Mindestens eine Terminkategorie muss aktiv sein.'),{status:400});return categories;}
export function categoryBookingSettings(settings,id){const categories=normalizeAppointmentCategories(settings.categories),category=id?categories.find(c=>c.id===id&&c.active):categories.find(c=>c.active);if(!category)throw Object.assign(new Error('Terminkategorie ist nicht verfügbar.'),{status:400});return{...settings,categories,category,defaultDurationMinutes:category.duration,offeredDurations:[category.duration]};}
